# 个人知识库全功能点自动化与人工测试文档

本文档详细描述了系统所有核心功能的测试覆盖面与验证方法。项目内包含了一个完整的端到端自动化测试脚本 `test_flow.js`（基于 JSDOM），可以快速检验核心链路的可靠性。

## 一、 自动化测试套件 (test_flow.js)

我们在根目录提供了基于 Node.js 的 E2E 测试脚本，用于测试无需人工干预的前端 DOM 逻辑流转。

### 运行方式
```bash
npm install jsdom
node test_flow.js
```

### 自动化验证模块
测试脚本会在内存中启动 HTTP Server，通过虚拟的 DOM 接口进行全流程序列点击和数据断言：
1. **渲染验证 (`TEST 1`)**: 断言 Dashboard 是否成功解析 `data.json` 或 `localStorage` 并挂载渲染了「最近更新」卡片列表。
2. **路由交互验证 (`TEST 2`)**: 断言使用 JS 触发卡片点击后，笔记 Modal 是否添加了 `.open` 样式并正确弹窗。
3. **编辑流转验证 (`TEST 3`)**: 断言点击「编辑」按钮后，UI 是否顺利切换隐藏了渲染视图，露出了含有源码的 `textarea`。
4. **存储与响应性验证 (`TEST 3`)**: 在输入框塞入模拟文本并点击保存后，断言 `modalNoteContent` 内部渲染的 HTML 是否包含了对应的修改；同时断言 `localStorage` 中是否也成功写入了修改后的 JSON 数据。
5. **动态修改 Frontmatter (`TEST 4`)**: 断言点击「置顶」图标后，程序能否通过正则表达式自动给文本头部加入 `---\npinned: true\n---`，并且确保该数据写入本地浏览器缓存。
6. **增量新建笔记 (`TEST 5`)**: 通过拦截 `window.prompt`，断言能够成功触发新笔记创建流程，进入带占位符的编辑模式，最终写入到数组列头中。

---

## 二、 人工/浏览器 UI 冒烟测试清单

除了自动化测试，以下清单列出了应当在真机浏览器中进行的 UI/UX 手动冒烟测试点：

### 1. 响应式布局测试
* **手机竖屏 (Width < 600px)**: 确认便当盒 (Bento Box) 布局由多列回退到了单列堆叠；确认底部的 FAB 悬浮按钮位置没有被安全区 (Safe Area) 遮挡；文件夹卡片在一行中至少展示两个。
* **桌面端宽屏 (Width > 768px)**: 确认 Dashboard 的 Bento CSS Grid 能够展示多列（置顶区占满、近期笔记横跨两列、文件夹列表布局紧凑）。

### 2. Obsidian 双向链接测试
* **正向解析**: 在笔记中写入 `[[某一篇笔记的名字]]`，保存后确认它在页面中变成了下划线超链接。
* **页面内跳转**: 点击生成的超链接，确认当前浮层立刻切换加载了目标笔记，并且标题、内容完全刷新（无需关闭再重新打开 Modal）。
* **容错机制**: 随意点击一个并不存在的链接，确认会弹出 `未找到笔记` 的提示框，而不会产生导致整个网页卡死的 JS 错误。

### 3. 断网/无后端容灾测试
1. 在代码中将 `supabaseClient` 相关初始化置为空，或者在浏览器 Network 面板开启 `Offline` 模式。
2. 刷新页面，确认页面在未报错的情况下自动 fallback 加载了离线缓存的数据。
3. 点击创建一篇新笔记并随意打几个字，点击保存。
4. **重新刷新浏览器页面**。
5. 确认刚才新建且没有存入数据库的笔记依然稳稳地出现在了“最近更新”中。此行为验证了完整的渐进式 Web APP (PWA) 级别的**本地高可用性**。

### 4. 极致阅读体验验证
* **语法高亮 (Highlight.js)**: 随便写入一段 Python 或 JavaScript 源码并套上 Markdown 的 ```代码块，保存后检查代码颜色是否变为 Atom One Dark 主题。
* **容灾设计**: 开启全局拦截掉 CDN 网络请求，确保即使代码高亮 JS 文件无法加载，网页也只会降级为普通无色字体，绝对不会出现因为 JS Error 导致的整体页面白屏情况。

---
以上功能点共同构成了此个人知识库的核心基石，确保了用户的数据不仅有云端同步能力，还具备绝不丢失的本地抗灾能力。
